# 羽言心理（PsyMap）运维维护文档

## 一、AI API 配置与费用

### 1.1 SiliconFlow 平台
- 官网：https://cloud.siliconflow.cn
- 账户管理：https://cloud.siliconflow.cn/account
- API Key：`sk-ozgipwvoghexlmpzriaesynaypyqjszqdllemcqzxvaokzqr`
- Base URL：`https://api.siliconflow.cn/v1`

### 1.2 当前账户余额（查询时间：2026年4月10日）

| 项目 | 数值 |
|------|------|
| 充值余额 | ¥8.58 |
| 赠送余额 | ¥0.00 |
| 总余额 | ¥8.58 |
| 账户状态 | 正常（normal） |

### 1.3 如何查询余额和用量

**方式一：网页控制台（推荐）**
1. 打开 https://cloud.siliconflow.cn
2. 登录账号
3. 点击右上角头像 → 「账户管理」
4. 可查看：当前余额、充值记录、消费明细、调用量统计

**方式二：API 查询**
```bash
curl -s -H "Authorization: Bearer sk-ozgipwvoghexlmpzriaesynaypyqjszqdllemcqzxvaokzqr" \
  "https://api.siliconflow.cn/v1/user/info"
```
返回 JSON 中关键字段：
- `chargeBalance`：充值余额（元）
- `balance`：赠送余额（元）
- `totalBalance`：总余额（元）
- `status`：账户状态（normal=正常）

**方式三：在终端快速查看余额**
```bash
curl -s -H "Authorization: Bearer sk-ozgipwvoghexlmpzriaesynaypyqjszqdllemcqzxvaokzqr" \
  "https://api.siliconflow.cn/v1/user/info" | python3 -c "
import sys,json
d=json.load(sys.stdin)['data']
print(f'充值余额: ¥{d[\"chargeBalance\"]}')
print(f'赠送余额: ¥{d[\"balance\"]}')
print(f'总余额:   ¥{d[\"totalBalance\"]}')
print(f'账户状态: {d[\"status\"]}')
"
```

### 1.4 充值方式
1. 登录 https://cloud.siliconflow.cn
2. 账户管理 → 充值
3. 支持支付宝/微信支付
4. 最低充值金额：¥10

### 1.5 使用的模型及费用

| 模型 | 用途 | 计费方式 | 注意事项 |
|------|------|----------|----------|
| `deepseek-ai/DeepSeek-OCR` | 拍照识别文字 | 按 token 计费 | 图片 base64 编码会消耗大量 input token |
| `Qwen/Qwen2.5-72B-Instruct` | AI评分、答案生成、题目结构化、学习计划、心理学知识、英文泛读 | 按 token 计费 | 主力模型，调用最频繁 |
| `FunAudioLLM/CosyVoice2-0.5B` | 语音合成（TTS） | 按字符/秒计费 | 制作音频时调用 |

### 1.6 费用监控要点
- 登录 SiliconFlow 控制台查看余额和用量
- 高频消耗场景：
  - 拍照导入题目（OCR + 结构化，每次约 2000-5000 token，约 ¥0.02-0.05）
  - AI评分（每次约 1000-2000 token，约 ¥0.01-0.02）
  - 心理学知识生成（每次约 3000-5000 token，约 ¥0.03-0.05）
  - 英文泛读生成（每次 5 篇文章，约 8000-15000 token，约 ¥0.08-0.15）
  - AI制定学习计划（约 1000-3000 token，约 ¥0.01-0.03）
- 低频消耗：答案自动生成（仅首次无答案时触发）
- 按当前余额 ¥8.58 估算，大约还能支撑：
  - AI评分约 400-800 次
  - 拍照导入约 170-400 次
  - 英文泛读约 55-100 次
- 建议每月检查一次余额，余额低于 ¥2 时及时充值
- SiliconFlow 新用户通常有免费额度，用完后需充值

### 1.7 费用预警阈值
| 余额 | 状态 | 建议操作 |
|------|------|----------|
| > ¥5 | 充足 | 正常使用 |
| ¥2 - ¥5 | 偏低 | 关注用量，减少英文泛读等高消耗操作 |
| < ¥2 | 紧张 | 尽快充值，否则可能影响 AI 评分等核心功能 |
| ¥0 | 欠费 | 所有 AI 功能不可用，需立即充值 |

### 1.4 API Key 更换流程
1. 在 SiliconFlow 控制台生成新 Key
2. App 内：我的 → 版本信息 → API 配置 → 修改 API Key
3. 或修改代码 `AiService.kt` 和 `PsyMapViewModel.kt` 中的默认值
4. 重新编译安装

### 1.5 模型更换注意事项
- OCR 模型：必须支持 vision（图片输入），当前 DeepSeek-OCR 效果最佳
- 文本模型：需要支持 JSON 格式输出，推荐 70B+ 参数量
- TTS 模型：需要 SiliconFlow 平台支持的语音模型
- 更换模型后需在 App 的 API 配置中修改，或改代码默认值

---

## 二、微信开放平台

### 2.1 账号信息
- AppID：`wx842e5368d23fb03c`
- AppSecret：`d54d1e37cdba63e6a236c59ca7b71cda`
- 包名：`com.psymap.app`
- Debug 签名 MD5：`2a1a58ef905713632434bd4fd1c02d91`

### 2.2 注意事项
- 微信开放平台审核通过后才能使用微信登录
- 如果更换签名证书（如发布 Release 版），需要在微信开放平台更新签名 MD5
- AppSecret 不要泄露到公开代码仓库（当前已硬编码在 `WeChatLogin.kt` 中，生产环境应移至服务端）

### 2.3 微信登录失败排查
1. 检查手机是否安装微信
2. 检查 AppID 和签名 MD5 是否与微信开放平台一致
3. 检查 `WXEntryActivity` 是否在 AndroidManifest 中正确注册
4. 检查网络是否可访问 `api.weixin.qq.com`

---

## 三、GitHub 仓库管理

### 3.1 仓库信息
- 仓库地址：https://github.com/yuyanpsy/psymap
- 账号：yuyanpsy@gmail.com
- 分支：main（唯一分支）

### 3.2 版本发布流程
1. 修改版本号（3处）：
   - `app/build.gradle.kts`：`versionCode` 和 `versionName`
   - `ProfilePage.kt`：`currentVersion` 常量和显示文本
2. 编译 APK：
   ```bash
   ANDROID_HOME=/Users/charles/Library/Android/sdk \
   JAVA_HOME=/Library/Java/JavaVirtualMachines/temurin-17.jdk/Contents/Home \
   ./gradlew assembleDebug --no-daemon
   ```
3. 更新 `version.json`（根目录）
4. 提交并推送：
   ```bash
   git add -A
   git commit -m "bump version to x.x.x"
   git tag vx.x.x
   git push origin main --tags
   ```
5. 在 GitHub Releases 页面：
   - 编辑对应 tag 的 release
   - 上传 `app/build/outputs/apk/debug/app-debug.apk`
   - 填写更新说明
6. 如果 tag 需要更新到最新 commit：
   ```bash
   git tag -f vx.x.x HEAD
   git push origin vx.x.x --force
   ```
   然后在 GitHub Release 页面点 "Update release" 刷新 source code 快照

### 3.3 GitHub Pages
- 地址：https://yuyanpsy.github.io/psymap
- 启用方式：仓库 Settings → Pages → Branch: main, Folder: / (root)
- 仓库从 private 切换为 public 后需要重新启用
- 当前分享链接已改为 GitHub Releases 页面，Pages 为备用

---

## 四、设备与构建环境

### 4.1 开发环境
| 项目 | 配置 |
|------|------|
| 操作系统 | macOS |
| Android SDK | `/Users/charles/Library/Android/sdk` |
| Java | Temurin JDK 17（`/Library/Java/JavaVirtualMachines/temurin-17.jdk`） |
| Gradle | 9.1.0（wrapper 管理） |
| Kotlin | Compose Plugin |
| 编译 SDK | 36 |
| 最低 SDK | 26（Android 8.0） |

### 4.2 测试设备
- 设备：小米手机
- 设备 ID：`64f1ce1d`
- 连接方式：USB 调试
- 注意：设备经常断开连接，安装前先 `adb devices` 确认

### 4.3 安装命令
```bash
/Users/charles/Library/Android/sdk/platform-tools/adb install -r \
  app/build/outputs/apk/debug/app-debug.apk
```

---

## 五、数据安全与备份

### 5.1 用户数据存储位置
- SharedPreferences：`/data/data/com.psymap.app/shared_prefs/`
  - `psymap.xml`：主数据（题库、题目、打卡、用户信息）
  - `psymap_reading.xml`：英文泛读文章
  - `psymap_marks.xml`：阅读标记
  - `psymap_psy_knowledge.xml`：心理学知识
- 音频文件：`Android/data/com.psymap.app/files/audio/`
- 备份文件：`Downloads/psymap_backup.json`

### 5.2 数据丢失风险
| 场景 | 影响 | 防护 |
|------|------|------|
| 卸载 App | SharedPreferences 和音频文件丢失 | 备份文件在 Downloads 目录，不受影响 |
| 清除 App 数据 | 同上 | 同上 |
| 换手机 | 所有数据丢失 | 需要先备份再恢复 |
| 备份文件被删 | 无法恢复 | 建议定期备份到云盘 |

### 5.3 备份建议
- 每周至少备份一次
- 重要操作（大量导入题目、考前冲刺）前先备份
- 备份文件 `psymap_backup.json` 建议额外拷贝到电脑或云盘

---

## 六、常见问题排查

### 6.1 AI 功能不可用
| 现象 | 可能原因 | 解决方案 |
|------|----------|----------|
| API错误(401) | API Key 无效或过期 | 更换 API Key |
| API错误(403) | 账户欠费或模型权限不足 | 充值或检查模型权限 |
| API错误(429) | 请求频率过高 | 等待后重试 |
| 网络错误: timeout | 网络不通或需要 VPN | 检查网络，开启 VPN |
| 评分解析失败 | AI 返回格式异常 | 代码已有降级处理，通常自动恢复 |

### 6.2 版本更新检查失败
| 现象 | 可能原因 | 解决方案 |
|------|----------|----------|
| 检查失败: timeout | GitHub raw 被墙 | 开启 VPN |
| 已是最新版本（但实际有新版） | version.json 未更新或 tag 未推送 | 检查 GitHub 上的 version.json |
| 下载链接 404 | Release 中未上传 APK | 手动上传 APK 到 Release Assets |

### 6.3 微信登录问题
| 现象 | 可能原因 | 解决方案 |
|------|----------|----------|
| 点击无反应 | 微信未安装 | 会自动弹出手动昵称输入 |
| 微信授权取消 | 用户取消或签名不匹配 | 检查 MD5 签名 |
| 获取 token 失败 | AppSecret 错误或 code 过期 | 检查配置 |

### 6.4 音频制作失败
| 现象 | 可能原因 | 解决方案 |
|------|----------|----------|
| API错误(403) | TTS 模型权限不足 | 检查 SiliconFlow 账户 |
| 语音引擎初始化失败 | 系统 TTS 未启用 | 手机设置中启用 TTS |
| 音频 0KB | API 返回空数据 | 降级为系统 TTS 朗读 |

---

## 七、敏感信息清单

以下信息不应泄露到公开渠道：

| 信息 | 当前位置 | 风险等级 |
|------|----------|----------|
| SiliconFlow API Key | `AiService.kt`、`PsyMapViewModel.kt` | 高（可被盗用消耗额度） |
| 微信 AppSecret | `WeChatLogin.kt` | 高（可伪造登录） |
| 管理员密码 | `PsyMapViewModel.kt`（YuYanPsy / 8monthpanda.） | 中 |

生产环境建议：
- API Key 和 AppSecret 移至服务端，App 通过自有后端中转
- 管理员验证改为服务端校验
- 当前为个人项目，风险可控，但如果用户量增长需要重构

## 二、Supabase 云端同步配置

### 2.1 Supabase 项目信息
- 控制台：https://supabase.com/dashboard
- 项目 URL：`https://YOUR_PROJECT_ID.supabase.co`
- Anon Key：`YOUR_ANON_KEY`

### 2.2 初始化步骤
1. 注册 Supabase 账号：https://supabase.com
2. 创建新项目（选择离你最近的区域，如 Singapore）
3. 进入 SQL Editor，执行 `PsyMapWeb/supabase-schema.sql` 中的全部 SQL
4. 在 Project Settings → API 中获取 `Project URL` 和 `anon public` key
5. 替换以下文件中的配置：
   - Web 端：`PsyMapWeb/js/supabase-config.js` 中的 `SUPABASE_URL` 和 `SUPABASE_ANON_KEY`
   - Android 端：`SupabaseClient.kt` 中的 `supabaseUrl` 和 `supabaseKey`

### 2.3 数据库表说明
| 表名 | 用途 |
|------|------|
| users | 用户信息 |
| banks | 题库 |
| questions | 题目（含错题/收藏/标签状态） |
| check_ins | 每日打卡记录 |
| daily_targets | 每日学习目标 |
| target_scores | 目标分数 |
| study_plans | 学习计划 |
| user_settings | AI配置等设置 |

### 2.4 同步机制
- Android 端：每次启动自动从云端拉取，每次数据变更自动后台推送
- Web 端：登录时从云端拉取，每次操作实时写入云端
- 两端通过相同的 nickname 关联同一用户
- 本地 SharedPreferences / localStorage 作为离线缓存

### 2.5 费用
- 免费计划：50k 月活用户、500MB 数据库、1GB 存储
- 对于个人考研备考使用完全足够
- 如需升级：Pro 计划 $25/月

### 2.6 数据迁移
- Web 端首次登录时，如果检测到旧的 localStorage 数据，会自动迁移到云端
- Android 端在「我的 → 云端同步 → 推送本地数据到云端」手动迁移

## 三、v0.1.0 版本更新记录（2026年4月17日）

### 3.1 Supabase 云端同步
- 新增 Supabase 后端，实现 Android 和 Web 端数据云端同步
- 数据库表：users、banks、questions、check_ins、daily_targets、target_scores、study_plans、user_settings
- 同步机制：upsert 合并模式（不覆盖），两端数据自动合并取最大值
- 同步码：6位数字，Android 端每次登录自动刷新，Web 端输入同步码关联账号
- 自动同步：每30秒检测本地变化并推送到云端
- 配置文件：
  - Web 端：`PsyMapWeb/js/supabase-config.js`
  - Android 端：`SupabaseClient.kt`
  - 数据库 Schema：`PsyMapWeb/supabase-schema.sql`
  - 迁移 SQL：`PsyMapWeb/supabase-migration-v2.sql`（sync_code 字段）

### 3.2 云端同步 UI
- Android 端：我的 → 云端同步（绿色标题表示已连接）
  - 显示同步码（6位数字）
  - 从云端拉取（合并模式）
  - 推送本地数据（本地无变化时按钮置灰）
  - 自动同步（30秒间隔）
- Web 端：我的 → 云端同步
  - 同步码显示
  - 拉取/推送按钮
  - 自动同步

### 3.3 思维导图功能
- 首页快捷入口「思维导图」替代原「我的题库」
- 二级列表页：显示已导入的思维导图文件（名称、类型、时间）
- 支持导入格式：PDF、图片（PNG/JPG）、FreeMind .mm
- 导入时可编辑文件名
- 文件持久化存储在 app 内部目录 `filesDir/mindmaps/`
- 两种查看模式（右上角切换）：
  - 大纲笔记：卡片式列表，按层级缩进，点击编辑，长按选择文本
  - 思维导图：WebView + SVG 树形布局，贝塞尔曲线连接，双指缩放，点击编辑
- 导出：仅 .mm 格式可导出，图片类型导出按钮置灰
- 编辑：点击节点内联编辑（大纲模式）或弹窗编辑（导图模式），保存回 .mm XML 文件
- 相关代码：`MindMapPage.kt`

### 3.4 Web 端改进
- TTS 音色选择：下拉框选择系统可用中文语音，默认普通话（排除粤语）
- 学习计划：今日进度显示改进，无目标也显示学习记录
- 版本号更新至 v0.1.0

### 3.5 其他改动
- Android 端云端同步菜单项去掉重复 emoji icon
- 同步码每次 Android 端登录时自动刷新
- Supabase SDK（Web 端）下载到本地，不依赖 CDN

## 四、v0.1.1 版本更新记录（2026年4月20日）

### 4.1 题目排序优化
- Question 模型新增 `createdAt` 字段
- `getQuestionsForBank()` 按创建时间降序排列，新添加的题目显示在最上方
- Web 端 Store 和 CloudStore 同步实现排序逻辑

### 4.2 OCR 识别增强
- OCR 提示词增加"使用中文简体，不要使用繁体字"
- AI 结构化提示词增加繁体转简体规则
- 识别流程优化：ML Kit 本地 OCR（秒级）→ AI 结构化（文本处理，消耗极小）
- AI 结构化不再受 `aiEnabled` 开关限制（仅 API Key 为空时降级）
- 文字量 > 6000 字时自动降级为本地解析
- 本地正则解析器全面重写：支持章节标题、题型分类、多种题号格式
- AI 结构化输出包含 6 个字段：question、answer、options、type、explanation、chapter
- 题型自动判断：选择题/简答题/论述题/案例分析题

### 4.3 Markdown 格式化
- 所有文本入口（addQuestion、updateQuestion）自动添加 Markdown 格式
- 数字编号（1. 2. 3.）自动加粗为要点标题
- 中文编号（一、二、三）转为三级标题
- 冒号前的短语自动加粗
- 已有 Markdown 格式的文本不重复处理
- AI 结构化 prompt 要求输出带 Markdown 格式的答案
- Web 端同步实现 `formatTextMarkdown()` 函数

### 4.4 软键盘适配
- AndroidManifest.xml 添加 `android:windowSoftInputMode="adjustResize"`
- 所有有文本输入的全屏 Dialog 页面添加 `.imePadding()`
- 涉及页面：编辑题目、添加题目、学习会话、考试答题、文件导入

### 4.5 底部按钮修复
- 所有 Dialog 内 Scaffold 的 bottomBar 移除，导航按钮改为放在滚动内容底部
- 底部留 32dp 间距，与"编辑题目"页面的"取消/保存"按钮实现方式一致
- 涉及页面：题目详情、学习会话、考试会话、文件导入
- 磨耳朵播放控制栏保留 bottomBar，bottom padding 设为 48dp
- Toast 重复弹出修复：删除 PsyMapApp.kt 全局监听器

### 4.6 首页入口恢复
- 首页题库列表上方新增"我的题库"标题栏
- 右侧放置"导入文件"和"新建题库"两个按钮

### 4.7 思维导图改进
- 导图视图从 SVG 纯文本渲染改为 HTML div 布局
- 节点自动根据内容计算高度，文字自动换行
- 支持 FreeMind `.mm` 文件中的 `<richcontent>` 标签（图文混排）
- 大纲模式支持 richcontent 的图文混排显示
- 编辑弹窗增大到 90% 宽度，支持富文本预览

### 4.8 Web 端同步改动
- `createQuestion` 新增 `createdAt` 字段
- `Store.getQuestionsForBank` 按 `createdAt` 降序排列
- `CloudStore.getQuestionsForBank` 同步排序
- `CloudStore._questionFromDb` 新增 `createdAt` 映射
- 题目详情页新增"✏️ 编辑"按钮，支持编辑题目/答案/选项/解析/标签/题型
- 编辑页支持"🖼 插入图片"（base64 data URL markdown 格式）
- `renderMarkdown` 增强：支持内联图片混排
- `Store.updateQuestion` 自动调用 `formatTextMarkdown` 格式化
