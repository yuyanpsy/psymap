# 羽言心理（PsyMap）技术开发文档

## 文档信息
| 项目 | 内容 |
|------|------|
| 项目名称 | 羽言心理（PsyMap） |
| 当前版本 | v0.0.7（versionCode: 7） |
| 开发周期 | 2025年3月 - 2026年4月 |
| 技术栈 | Kotlin + Jetpack Compose + Material Design 3 |
| 最低SDK | Android 8.0（API 26） |
| 编译SDK | 36 |
| 构建工具 | Gradle 9.1 + Kotlin Compose Plugin |

---

## 一、项目架构

### 1.1 整体架构
采用 MVVM 架构模式：
- **View 层**：Jetpack Compose 声明式 UI，全部使用 `@Composable` 函数
- **ViewModel 层**：`PsyMapViewModel`（继承 `AndroidViewModel`），管理所有业务状态
- **Model 层**：`Models.kt` 定义数据类，`AiService` 封装 AI API 调用
- **数据持久化**：SharedPreferences + Gson JSON 序列化

### 1.2 源码结构
```
app/src/main/java/com/psymap/app/
├── MainActivity.kt          # 入口 Activity + 主题定义
├── PsyMapApp.kt             # 主界面框架（底部导航、拍照入口、全局Loading）
├── Models.kt                # 数据模型（User, Question, QuestionBank, DailyCheckIn 等）
├── PsyMapViewModel.kt       # 核心 ViewModel（877行，管理所有业务逻辑和状态）
├── AiService.kt             # AI API 封装（OCR、评分、文本生成、TTS）
├── HomePage.kt              # 首页（搜索、倒计时、8功能入口、题库列表、更多知识、音频等）
├── DiscoverPage.kt          # 题库Tab（左侧分类 + 右侧题目列表 + 标签筛选）
├── PracticePage.kt          # 学习Tab（学习会话、错题本、收藏本）
├── StudySessionPage.kt      # 学习会话全屏页面（答题、AI评分、上下题切换）
├── ProfilePage.kt           # 我的Tab（用户信息、目标分数、统计、备份恢复、版本检查）
├── LoginPage.kt             # 登录页（微信登录 + 手动昵称输入）
├── Dialogs.kt               # 公共弹窗组件（题目详情、导入、API配置等）
├── WeChatLogin.kt           # 微信登录 SDK 封装
└── wxapi/
    └── WXEntryActivity.kt   # 微信回调 Activity
```

### 1.3 依赖库
| 库 | 版本 | 用途 |
|----|------|------|
| Jetpack Compose BOM | 2024.12.01 | UI 框架 |
| Material3 | BOM管理 | Material Design 3 组件 |
| Material Icons Extended | BOM管理 | 扩展图标库 |
| Navigation Compose | 2.8.5 | 页面导航（实际未深度使用，用状态切换） |
| Lifecycle ViewModel Compose | 2.8.7 | ViewModel 集成 |
| OkHttp | 4.12.0 | HTTP 网络请求 |
| Gson | 2.11.0 | JSON 序列化/反序列化 |
| Coil Compose | 2.5.0 | 网络图片加载（微信头像） |
| WeChat SDK | 6.8.28 | 微信登录 |

---

## 二、核心技术实现

### 2.1 状态管理
全部使用 Compose 的 `mutableStateOf` 进行响应式状态管理：
```kotlin
var questionBanks by mutableStateOf(listOf<QuestionBank>())
var questions by mutableStateOf(listOf<Question>())
var checkInRecords by mutableStateOf(listOf<DailyCheckIn>())
```
异步操作使用 `MutableStateFlow`：
```kotlin
private val _isLoading = MutableStateFlow(false)
val isLoading = _isLoading.asStateFlow()
```
所有状态变更在主线程执行，AI 回调通过 `viewModelScope.launch(Dispatchers.Main)` 或 `Handler(Looper.getMainLooper())` 切回主线程。

### 2.2 数据持久化方案
选择 SharedPreferences + Gson 而非 Room 数据库，原因：
- 数据量可控（题库级别，非海量数据）
- 开发速度快，无需定义 DAO/Entity
- 备份恢复简单（直接 JSON 导出导入）

存储 key 映射：
| Key | 数据 |
|-----|------|
| `questionBanks` | 题库列表 JSON |
| `questions` | 题目列表 JSON |
| `checkIns` | 打卡记录 JSON |
| `studyPlans` | 学习计划 JSON |
| `dailyTargets` | 每日目标 JSON |
| `user` | 用户信息 JSON |
| `targetPoliticsScore` | 政治目标分数 (Int) |
| `targetEnglishScore` | 英语目标分数 (Int) |
| `targetPsyScore` | 专业综合目标分数 (Int) |
| `apiKey` / `apiBaseUrl` / `modelName` | AI API 配置 |

额外 SharedPreferences 文件：
- `psymap_reading`：英文泛读文章数据
- `psymap_marks`：阅读标记（重点词汇、长难句）
- `psymap_psy_knowledge`：心理学知识生成内容

### 2.3 数据备份与恢复

备份使用 `MediaStore` API 写入 `Downloads/psymap_backup.json`，确保卸载 App 后数据不丢失：
```kotlin
val values = ContentValues().apply {
    put(MediaStore.Downloads.DISPLAY_NAME, "psymap_backup.json")
    put(MediaStore.Downloads.MIME_TYPE, "application/json")
    put(MediaStore.Downloads.IS_PENDING, 1)
}
val uri = resolver.insert(MediaStore.Downloads.EXTERNAL_CONTENT_URI, values)
```

恢复支持两种方式：
1. MediaStore 自动查找 Downloads 目录中的 `psymap_backup.json`
2. 系统文件选择器 `ActivityResultContracts.OpenDocument()`，用户手动选择文件

备份内容（v4格式）包含：题库、题目（含所有标签和统计）、打卡记录、学习计划、每日目标、用户信息、API配置、目标分数、英文泛读文章、阅读标记、心理学知识。

---

## 三、AI 服务集成

### 3.1 API 架构
使用 SiliconFlow 平台提供的 AI API，基于 OpenAI 兼容接口格式：

| 模型 | 用途 | 模型名 |
|------|------|--------|
| OCR 模型 | 拍照识别文字 | `deepseek-ai/DeepSeek-OCR` |
| 文本模型 | 评分、结构化、生成答案 | `Qwen/Qwen2.5-72B-Instruct` |
| TTS 模型 | 语音合成 | `FunAudioLLM/CosyVoice2-0.5B` |

API Base URL: `https://api.siliconflow.cn/v1`

### 3.2 OCR 两步识别流程
拍照导入题目采用两步流程，解决单模型识别不准确的问题：

**Step 1 - DeepSeek-OCR 提取原始文字**：
- 图片压缩至 1600px 以内，JPEG 质量 90%
- Base64 编码后通过 vision API 发送
- 提示词：`"OCR识别图片中的所有文字内容，完整输出，不要遗漏。"`

**Step 2 - Qwen2.5-72B 结构化**：
- 将 OCR 原始文字发送给文本模型
- 要求输出 JSON 数组：`[{"question":"题目","answer":"答案","options":["A.xx"]}]`
- 选择题自动识别选项，非选择题 options 为空数组

### 3.3 AI 评分机制
主观题评分分两种情况：

**有标准答案**：直接调用评分 API，按踩分点给分（满分100）
**无标准答案**：AI 先生成标准答案 → 保存到本地 → 再用该答案评分

评分结果 JSON 解析带容错：
```kotlin
// 主解析
val map = gson.fromJson(cleanJson(result), ...)
val score = (map["score"] as? Double)?.toInt()

// 降级：正则提取
val scoreMatch = Regex("\"score\"\\s*:\\s*(\\d+)").find(result)
```

分数 ≥ 60 判定为正确，自动更新错题本状态。

### 3.4 JSON 清洗
AI 返回经常包含 markdown 代码块包裹，`cleanJson()` 函数统一处理：
```kotlin
fun cleanJson(raw: String): String {
    val codeBlockRegex = Regex("```(?:json)?\\s*\\n?([\\s\\S]*?)\\n?```")
    val match = codeBlockRegex.find(raw)
    return match?.groupValues?.get(1)?.trim() ?: raw.trim()
}
```

### 3.5 TTS 语音合成
音频制作采用降级策略：
1. 优先调用 SiliconFlow CosyVoice2 API 生成音频文件
2. API 不可用时降级为 Android 系统 TTS `speak()` 实时朗读
3. 小米设备不支持 `synthesizeToFile()`，只能实时播放

音频文件存储路径：`context.getExternalFilesDir(null)/audio/`

---

## 四、微信登录集成

### 4.1 配置信息
- AppID: `wx842e5368d23fb03c`
- 回调 Activity: `com.psymap.app.wxapi.WXEntryActivity`
- AndroidManifest 中需要 `<queries>` 声明微信包名以满足 Android 11+ 包可见性要求

### 4.2 登录流程
```
用户点击微信登录 → WeChatLogin.login() 发起 SendAuth.Req
→ 微信 App 授权 → WXEntryActivity 收到 code
→ getAccessToken(code) 换取 access_token + openid
→ getUserInfo() 获取昵称、头像
→ loginAsNormalUser() 保存用户信息（所有微信用户自动获得管理员权限）
```

### 4.3 降级方案
微信未安装时，显示手动输入昵称的输入框，用户输入后直接以管理员身份登录。

---

## 五、打卡系统设计

### 5.1 打卡规则
- 每个题库有独立的每日目标题数
- 所有有目标的题库都完成当日目标 → 当天打卡成功
- `submitAnswer()` → `recordBankProgress()` → 更新 `DailyCheckIn.bankProgress`

### 5.2 目标快照机制
修改学习计划不影响历史打卡记录。每次记录进度时，将当天的总目标数保存到 `DailyCheckIn.targetCount`：
```kotlin
val totalTarget = dailyTargets.values.filter { it > 0 }.sum().coerceAtLeast(
    existing?.targetCount ?: 0  // 不降低已记录的目标
)
```
判断历史某天是否打卡时，使用该记录自身的 `targetCount`，而非当前的 `dailyTargets`。

### 5.3 连续打卡计算
从今天开始往前逐天检查，遇到未打卡的日期即停止：
```kotlin
private fun calculateConsecutiveDays(): Int {
    val cal = Calendar.getInstance()
    var count = 0
    while (true) {
        val dateStr = dateFormat.format(cal.time)
        val dayRecord = checkInRecords.find { it.date == dateStr }
        if (dayRecord != null && isDayCheckedIn(dayRecord)) {
            count++; cal.add(Calendar.DAY_OF_YEAR, -1)
        } else break
    }
    return count
}
```

### 5.4 打卡日历
全屏 Dialog，支持月份前后切换，颜色编码：
- 🟢 绿色：已完成打卡
- 🟠 橙色：部分完成
- ⚪ 灰色：未打卡

---

## 六、UI 设计实现

### 6.1 MIUI 风格设计规范
| 元素 | 实现 |
|------|------|
| 主色调 | `Color(0xFFEF6C00)` 橙色 |
| 渐变背景 | `Brush.verticalGradient(listOf(Color(0xFFEF6C00), Color(0xFFFF9800)))` |
| 卡片 | `RoundedCornerShape(12.dp)` + 白色背景 + 无阴影 |
| 底部导航 | 5个Tab + 中间突出拍照按钮（橙色圆形） |
| 全屏页面 | `Dialog(onDismissRequest, properties=DialogProperties(usePlatformDefaultWidth=false))` + `Scaffold` + `TopAppBar` |

### 6.2 全屏 Dialog 方案
将大部分功能页面从 AlertDialog 改为全屏 Dialog+Scaffold，解决弹窗尺寸受限问题：
```kotlin
Dialog(
    onDismissRequest = onDismiss,
    properties = DialogProperties(usePlatformDefaultWidth = false)
) {
    Scaffold(
        topBar = {
            TopAppBar(
                title = { Text("页面标题") },
                navigationIcon = { IconButton(onClick = onDismiss) { Icon(Icons.Default.ArrowBack, ...) } }
            )
        }
    ) { padding -> ... }
}
```

保留 AlertDialog 的场景：快速选择类（编辑每日目标、创建题库、选择题型等）。

### 6.3 标签展示
使用 `FlowRow` 替代 `Row` 展示标签，避免滚动条：
```kotlin
FlowRow(horizontalArrangement = Arrangement.spacedBy(6.dp)) {
    AssistChip(onClick = {}, label = { Text("常考🔥") })
    AssistChip(onClick = {}, label = { Text("多背📖") })
}
```
学习界面中标签为只读 `AssistChip`，不可编辑。

---

## 七、版本更新机制

### 7.1 version.json 格式
存放在 GitHub 仓库根目录：
```json
{
  "versionCode": 7,
  "versionName": "0.0.7",
  "downloadUrl": "https://github.com/yuyanpsy/psymap/releases/download/v0.0.7/app-debug.apk",
  "changelog": "更新说明"
}
```

### 7.2 检查更新逻辑
从 `https://raw.githubusercontent.com/yuyanpsy/psymap/main/version.json` 获取最新版本信息，与当前内置版本号比较：
```kotlin
val latestVersion = (map["versionName"] as? String) ?: (map["version"] as? String) ?: ""
val currentVersion = "0.0.7"
if (latestVersion.isNotBlank() && latestVersion != currentVersion) {
    // 跳转浏览器下载
    context.startActivity(Intent(Intent.ACTION_VIEW, Uri.parse(downloadUrl)))
}
```

### 7.3 发布流程
1. 修改 `build.gradle.kts` 中 `versionCode` 和 `versionName`
2. 修改 `ProfilePage.kt` 中硬编码的 `currentVersion` 和显示版本号
3. 编译：`ANDROID_HOME=... JAVA_HOME=... ./gradlew assembleDebug --no-daemon`
4. 更新根目录 `version.json`
5. 推送代码：`git -C "AI Projects/PsyMap" push origin main --tags`
6. GitHub Releases 页面上传 APK 到 Assets

---

## 八、开发过程中解决的问题

### 8.1 OCR 识别方案迭代
| 阶段 | 方案 | 问题 |
|------|------|------|
| v1 | Qwen2.5-VL-72B 直接识别 | 识别结果完全错误，乱码 |
| v2 | Qwen3-VL-32B | 同样不准确 |
| v3 | Android 本地 OCR | 能力不足，无法结构化 |
| v4（最终） | DeepSeek-OCR 提取文字 + Qwen2.5-72B 结构化 | 两步流程，准确率大幅提升 |

### 8.2 AI 评分 JSON 解析失败
AI 返回的 JSON 经常被 markdown 代码块包裹（```json ... ```），导致 Gson 解析失败。
解决：`cleanJson()` 函数用正则去除代码块包裹，并增加正则降级提取 score/feedback。

### 8.3 TTS 音频生成失败
- SiliconFlow CosyVoice2 API 返回 403 错误 → 降级为系统 TTS
- 小米设备 `synthesizeToFile()` 不支持 → 改用 `speak()` 实时朗读
- 生成的音频文件 0KB → 检查发现 API 返回空数据，增加文件大小校验

### 8.4 打卡天数不一致
问题：打卡日历显示已打卡，但首页显示连续打卡 0 天。
原因：`totalCheckInDays` 和 `consecutiveCheckInDays` 缓存在 `User` 对象中，未及时刷新。
解决：
1. 增加 `totalCheckedDays` 和 `consecutiveCheckedDays` 计算属性，直接从 `checkInRecords` 实时计算
2. 每次 `recordBankProgress()` 后调用 `refreshCheckInStats()` 刷新

### 8.5 修改学习计划影响历史打卡
问题：修改每日目标后，之前已完成的打卡变成未完成。
解决：每日打卡记录保存当天的 `targetCount` 快照，判断历史打卡时使用记录自身的目标值，不受后续修改影响。

### 8.6 版本检查逻辑 Bug
| Bug | 原因 | 修复 |
|-----|------|------|
| 检查不到新版本 | `version.json` 字段名 `versionName` 但代码读 `version` | 兼容读取两个字段名 |
| 永远提示有新版本 | 比较逻辑硬编码 `!= "0.0.3"` | 改为与当前版本 `currentVersion` 比较 |
| 版本显示错误 | 硬编码 `"v0.0.6"` 未更新 | 更新为 `"v0.0.7"` |

### 8.7 微信登录头像不显示
问题：微信登录后头像不显示。
原因：微信头像是 URL，需要网络图片加载库。
解决：引入 Coil 库（`io.coil-kt:coil-compose:2.5.0`），使用 `AsyncImage` 组件加载。

### 8.8 数据恢复丢失题目
问题：备份显示"5个题库，20道题目"，恢复后只有题库没有题目。
原因：备份 JSON 中 `questions` 字段序列化/反序列化类型不匹配。
解决：恢复时统一使用 `gson.toJson(map["questions"])` 先转 JSON 字符串再反序列化，避免类型丢失。

### 8.9 错题本计数不准
问题：显示"全部错题2道"但点开有30个。
原因：`questionCount` 字段未同步更新。
解决：错题列表直接用 `questions.filter { it.isInWrongBook }` 实时过滤，不依赖缓存计数。

### 8.10 GitHub Pages 404
问题：分享链接 `https://yuyanpsy.github.io/psymap` 返回 404。
原因：仓库从 private 切换为 public 后 GitHub Pages 需要重新启用。
解决：分享链接改为 `https://github.com/yuyanpsy/psymap/releases`，直接指向 Releases 页面。

### 8.11 多选题无法多选
问题：政治多选题只能选一个选项。
原因：选项状态用 `selectedOption: Int`（单值），点击直接覆盖。
解决：新增 `selectedOptions: Set<Int>` 状态，多选题点击切换选中/取消。答案判定支持 "ABD"/"A,B,D"/"A B D" 等格式，将用户选择的字母集合与正确答案字母集合比较。

### 8.12 正确率计算不一致
问题：学习Tab显示92%，我的Tab显示33%，同一题库数据不同。
原因：
1. `getSubjectStats()` 按科目合并时后面的题库覆盖前面的（`result[subject] = ...` 而非累加）
2. 正确率用 `correctCount / (correctCount + wrongCount)` 是答题次数，不是题目数
解决：
- 我的Tab改为按题库维度显示（`getBankStats()`），每道题只算一次，取最新结果（不在错题本=答对）
- 学习Tab改为当日正确率，使用 `DailyCheckIn.bankStudiedIds`/`bankCorrectIds` 按题目ID去重
- "学习统计"重命名为"累计正确率"

### 8.13 今日学习题数重复计算
问题：普心11题显示"今日已学习21题"。
原因：`bankProgress` 记录的是答题次数，重复答同一题会累加。
解决：`DailyCheckIn` 新增 `bankStudiedIds`（去重题目ID集合）和 `bankCorrectIds`（去重正确题目ID集合），同一题多次作答只算一次，正确与否取最新结果。

---

## 九、构建与部署

### 9.1 构建环境
```
ANDROID_HOME=/Users/charles/Library/Android/sdk
JAVA_HOME=/Library/Java/JavaVirtualMachines/temurin-17.jdk/Contents/Home
```

### 9.2 构建命令
```bash
# 编译 Debug APK
ANDROID_HOME=/Users/charles/Library/Android/sdk \
JAVA_HOME=/Library/Java/JavaVirtualMachines/temurin-17.jdk/Contents/Home \
./gradlew assembleDebug --no-daemon

# APK 输出路径
app/build/outputs/apk/debug/app-debug.apk
```

### 9.3 安装到设备
```bash
# 查看连接设备
adb devices

# 安装 APK（设备ID: 64f1ce1d，小米手机）
adb install -r app/build/outputs/apk/debug/app-debug.apk
```

### 9.4 Git 推送
```bash
git -C "AI Projects/PsyMap" add -A
git -C "AI Projects/PsyMap" commit -m "描述"
git -C "AI Projects/PsyMap" push origin main --tags
```

### 9.5 微信开放平台配置
- Debug 签名 MD5: `2a1a58ef905713632434bd4fd1c02d91`
- 包名: `com.psymap.app`
- 需在微信开放平台注册应用并通过审核

---

## 十、版本迭代记录

| 版本 | 主要变更 |
|------|----------|
| v0.0.1 | 初始版本：登录、题库、学习、打卡基础功能 |
| v0.0.2 | 删除设置入口，API配置移入版本信息，分享App功能 |
| v0.0.3 | 头像可点击查看账号详情，版本检查功能上线 |
| v0.0.4 | 修复音频制作、版本检查逻辑 |
| v0.0.5 | 更多知识（心理学知识+英文泛读）、删除拍照搜题 |
| v0.0.6 | 全屏页面重构、打卡日历月份切换、AI制定计划、学习计划目标快照 |
| v0.0.7 | MIUI风格优化、Markdown渲染、AI自动生成答案、版本检查Bug修复、分享链接更新 |
| v0.0.7+ | 考研日期修正(2026-12-19)、多选题支持、正确率计算修正(按题目去重)、今日学习去重计数、累计正确率重命名、标签宽度自适应 |
| v0.0.8 | 考一考模块（模拟考试+倒计时+拍照手写评分）、解析栏（所有题型）、AI开关控制付费功能、OCR改为ML Kit+腾讯云手写OCR、TTS改为腾讯云+Edge TTS双引擎、文件导入本地解析、题型多选筛选、前台Service后台播放、全局迷你播放条、图文混排、NumberStepper组件、整体计划导入、每日学习限制 |
| v0.1.1 | 题目按创建时间降序排列、OCR识别增强（ML Kit+AI结构化三层策略）、全文本自动Markdown格式化、软键盘适配imePadding、底部按钮统一移入滚动内容、首页恢复导入文件/新建题库入口、思维导图节点自适应高度+图文混排、Web端题目编辑+图片插入+排序同步 |
